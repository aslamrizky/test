# This file is filtered by Maven (resource filtering) at build time.
# The placeholders below are replaced with the real values from the POM /
# command line, so the running jar always knows exactly which version and
# build it is.
app.version=${project.version}
app.buildNumber=${build.number}
app.buildTimestamp=${build.timestamp}
